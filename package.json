{
  "name": "react-node-sql",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "frontend": "cd frontend && yarn start",
    "api": "cd api && yarn dev",
    "dev": "concurrently \"yarn run api\" \"yarn run frontend\""
  },
  "devDependencies": {
    "concurrently": "^5.3.0"
  }
}
